#include <core/image.h>
#include <fstream>
#include <sstream>
#include <core/image_evaluator.h>

namespace naivebayes {
    void ImageEvaluator::Classify(Image image_to_classify, vector<Image> training_images) {

    }
}
// namespace naivebayes